# Main User Frontend
vetlinked.in, vetlinked.com {
    # Route Socket.IO and REST API requests to backend
    handle_path /api/* {
        reverse_proxy backend:5000
    }
    handle /socket.io/* {
        reverse_proxy backend:5000
    }
    
    # Otherwise route to frontend
    handle {
        reverse_proxy ets:3002
    }
}

# Admin Frontend
admin.vetlinked.in, admin.vetlinked.com {
    reverse_proxy admin:3001
}

# Dedicated Backend API Subdomains
api.vetlinked.in, api.vetlinked.com {
    reverse_proxy backend:5000
}
